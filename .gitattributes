# Normalize line endings to LF in the repo; check out native on each OS.
* text=auto eol=lf

# Treat these as binary (no diff/normalization).
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.mp4 binary
*.webp binary
