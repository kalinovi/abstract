-- Optional seed data for local development.
-- Run automatically by `supabase db reset`.
--
-- NOTE: artworks require real files in the 'artworks' storage bucket and a
-- real auth user as the author, so we keep the seed minimal. After creating
-- your first account through the app, upload a few pieces to populate the feed.

-- (Intentionally empty — sign up in the app and upload artwork to get started.)
